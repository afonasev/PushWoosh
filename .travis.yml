language: python
python:
  - "3.3"
  - "3.4"
  - "3.5"

install:
  - pip install pytest pytest-coverage codeclimate-test-reporter
  - pip install flake8 flake8-import-order

script:
  - py.test --cov=pushwoosh
  - flake8

after_success:
  - codeclimate-test-reporter
